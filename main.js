'use strict';

{

    //push,pop,unshift,shift
    const scores =  [70, 90, 80, 85,];

    scores.push(100,200);

    scores.forEach((score, index) => {
        console.log(`${index}: ${score}`);
    });


}
