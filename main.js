'use strict';

{

    //push,pop,unshift,shift
    const scores =  [70, 90, 80, 85,];

    scores.push(100,200);

    for (let i  = 0; i< scores.length; i++ ) {
        console.log(scores[i]);
    }


}
